{
  "name": "sessiontrail",
  "version": "0.6.3",
  "description": "Runtime behavior review for AI agent sessions.",
  "type": "module",
  "bin": {
    "sessiontrail": "./bundle/index.js"
  },
  "files": [
    "bundle",
    "action.yml",
    "LICENSE",
    "README.md"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "bundle": "ncc build src/index.ts -o bundle --minify",
    "prepublishOnly": "npm run bundle",
    "pretest": "npm run build",
    "test": "node --test test/*.test.mjs"
  },
  "dependencies": {
    "agent-gov-core": "^1.2.1"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "@vercel/ncc": "^0.38.4",
    "typescript": "^5.9.3"
  },
  "license": "MIT"
}
