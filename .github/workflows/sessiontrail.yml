name: SessionTrail

on:
  pull_request:

permissions:
  contents: read

jobs:
  behavior-review:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - uses: ./
        with:
          transcript-dir: test/fixtures
          repo: .
          fail-on: none
